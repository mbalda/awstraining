namespace BMICalculator.Models
{
    public enum CalculationDescription
    {
        ExtremeUnderweight,
        Underweight,
        Normal,
        Overweight,
        Obesity,
        ExtremeObesity,
        NotCalculated
    }
}
